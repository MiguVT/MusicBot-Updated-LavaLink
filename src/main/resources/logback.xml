<!-- author: Kaidan Gustave -->
<configuration>

    <appender name="Simple" class="ch.qos.logback.core.ConsoleAppender">
        <encoder>
            <!-- Pattern -->
            <pattern>
                %nopex[%d{HH:mm:ss}] [%level] [%logger{0}]: %msg%n%ex
            </pattern>
        </encoder>
    </appender>

    <!-- Enable trace logging for JDA -->
    <!-- <logger name="net.dv8tion.jda" level="TRACE"/> -->

    <root level="INFO">
        <appender-ref ref="Simple"/>
    </root>

</configuration>
